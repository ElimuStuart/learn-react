const {NavBar} = "NavBar"
const {Footer} = "Footer"
const {MainContent} = "MainContent"

const App = () => {
    return (
        <NavBar />
        <MainContent />
        <Footer />
    )
};

export default App;